# APIs

- Pagination endpoints don't guarantee ordering without explicit ORDER BY
- Rate limits often apply per-endpoint, not just per-account
- Webhook signatures should be verified before parsing the payload
